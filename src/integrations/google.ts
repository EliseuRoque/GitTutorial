import type { Activity } from '../types';
const DISCOVERY='https://www.googleapis.com';
export async function exportCalendar(activity:Activity,token:string){return fetch(`${DISCOVERY}/calendar/v3/calendars/primary/events`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({summary:activity.title,description:activity.description,location:activity.location,start:{dateTime:`${activity.date}T${activity.start}:00`},end:{dateTime:`${activity.date}T${activity.end}:00`}})}).then(r=>r.json())}
export async function importCalendar(token:string,timeMin:string,timeMax:string){return fetch(`${DISCOVERY}/calendar/v3/calendars/primary/events?timeMin=${encodeURIComponent(timeMin)}&timeMax=${encodeURIComponent(timeMax)}&singleEvents=true`,{headers:{Authorization:`Bearer ${token}`}}).then(r=>r.json())}
export async function exportToSheets(token:string,spreadsheetId:string,title:string,rows:(string|number|boolean)[][]){const meta=await fetch(`${DISCOVERY}/sheets/v4/spreadsheets/${spreadsheetId}:batchUpdate`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({requests:[{addSheet:{properties:{title}}}]})}).then(r=>r.json());const sheet=meta.replies?.[0]?.addSheet?.properties?.title||title;return fetch(`${DISCOVERY}/sheets/v4/spreadsheets/${spreadsheetId}/values/${encodeURIComponent(sheet)}!A1:append?valueInputOption=USER_ENTERED`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({values:rows})})}
