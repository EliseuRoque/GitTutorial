import { useEffect } from 'react';import { AlertTriangle, CheckCircle2, Clock } from 'lucide-react';import { Bar, BarChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';import { Card } from '../components/ui/Card';import { completed, pending, totals, upcoming, usePlanner } from '../store/plannerStore';
export default function Dashboard(){const s=usePlanner();useEffect(()=>{s.runAlerts()},[]);const data=totals(s.activities,s.categories);return <div className="space-y-6"><h2 className="text-3xl font-bold">Dashboard</h2><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{data.map(d=><Card key={d.name}><p className="text-sm text-muted-foreground">Horas de {d.name}</p><b className="text-3xl" style={{color:d.color}}>{d.hours.toFixed(1)}h</b></Card>)}</div><div className="grid gap-4 md:grid-cols-3"><Card><CheckCircle2/>Concluídas: <b>{completed(s.activities)}</b></Card><Card><Clock/>Pendentes: <b>{pending(s.activities)}</b></Card><Card><AlertTriangle/>Importantes atrasadas: <b>{s.alerts.filter(a=>!a.read).length}</b></Card></div><Card><h3 className="font-bold">Alertas</h3>{s.alerts.length? s.alerts.map(a=><p key={a.id} className="py-1">{a.message}</p>):<p className="text-muted-foreground">Nenhum alerta ativo.</p>}</Card><Card><h3 className="font-bold">Próximas atividades</h3>{upcoming(s.activities).map(a=><p key={a.id}>{a.date} {a.start} — {a.title}</p>)}</Card><Card className="h-80"><ResponsiveContainer><BarChart data={data}><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey="hours" fill="#2563eb"/></BarChart></ResponsiveContainer></Card></div>}
