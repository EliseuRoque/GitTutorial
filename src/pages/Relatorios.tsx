import { Bar, BarChart, Line, LineChart, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';import { Button } from '../components/ui/Button';import { Card } from '../components/ui/Card';import { completed, pending, totals, usePlanner } from '../store/plannerStore';import { downloadCsv, downloadXlsx } from '../utils/export';
export default function Relatorios(){const s=usePlanner();const cat=totals(s.activities,s.categories);const week=s.activities.map(a=>({date:a.date,hours:1}));return <div className="space-y-4"><h2 className="text-3xl font-bold">Relatórios e exportação</h2><div className="flex gap-2"><Button onClick={()=>downloadCsv('atividades',s.activities)}>Exportar CSV</Button><Button onClick={()=>downloadXlsx('planner-360',{Atividades:s.activities,Categorias:s.categories,Leituras:s.readings,Projetos:s.projects})}>Exportar XLSX</Button></div><div className="grid gap-4 lg:grid-cols-2"><Card className="h-72"><h3>Horas por categoria</h3><ResponsiveContainer><PieChart><Pie data={cat} dataKey="hours" nameKey="name" fill="#2563eb" label/><Tooltip/></PieChart></ResponsiveContainer></Card><Card className="h-72"><h3>Horas por semana/mês</h3><ResponsiveContainer><BarChart data={week}><XAxis dataKey="date"/><YAxis/><Tooltip/><Bar dataKey="hours" fill="#0f766e"/></BarChart></ResponsiveContainer></Card><Card><h3>Atividades</h3><p>Concluídas: {completed(s.activities)}</p><p>Pendentes: {pending(s.activities)}</p></Card><Card className="h-72"><h3>Evolução da leitura</h3><ResponsiveContainer><LineChart data={s.readings.map(r=>({date:r.date,pages:r.endPage-r.startPage+1}))}><XAxis dataKey="date"/><YAxis/><Tooltip/><Line dataKey="pages" stroke="#db2777"/></LineChart></ResponsiveContainer></Card></div></div>}
