import { Bar, BarChart, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';import { Button } from '../components/ui/Button';import { Card } from '../components/ui/Card';import { completed, pending, totals, usePlanner } from '../store/plannerStore';import { downloadCsv, downloadXlsx } from '../utils/export';
export default function Relatorios(){const s=usePlanner();const cat=totals(s.activities,s.categories);const byDay=s.activities.map(a=>({date:a.date,hours:1}));return <div className="space-y-4"><h2 className="text-3xl font-bold">Relatórios e exportação</h2><div className="flex flex-wrap gap-2"><Button onClick={()=>downloadCsv('atividades',s.activities)}>Exportar CSV</Button><Button onClick={()=>downloadXlsx('planner-360',{Atividades:s.activities,Categorias:s.categories,Rascunhos:s.drafts})}>Exportar XLSX</Button></div><div className="grid gap-4 lg:grid-cols-2"><Card className="h-72"><h3>Horas por categoria</h3><ResponsiveContainer><PieChart><Pie data={cat} dataKey="hours" nameKey="name" fill="#2563eb" label/><Tooltip/></PieChart></ResponsiveContainer></Card><Card className="h-72"><h3>Atividades por data</h3><ResponsiveContainer><BarChart data={byDay}><XAxis dataKey="date"/><YAxis/><Tooltip/><Bar dataKey="hours" fill="#0f766e"/></BarChart></ResponsiveContainer></Card><Card><h3>Resumo</h3><p>Concluídas: {completed(s.activities)}</p><p>Pendentes: {pending(s.activities)}</p></Card></div></div>}
