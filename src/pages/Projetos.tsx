import { useState } from 'react';import { Button } from '../components/ui/Button';import { Card } from '../components/ui/Card';import { Input } from '../components/ui/Input';import { usePlanner } from '../store/plannerStore';
export default function Projetos(){const s=usePlanner();const [name,setName]=useState('');return <div className="space-y-4"><h2 className="text-3xl font-bold">Projetos acadêmicos</h2><Card className="flex flex-wrap gap-2"><Input className="max-w-xs" value={name} onChange={e=>setName(e.target.value)} placeholder="Nome"/><select id="ptype" className="rounded-xl border bg-background p-2"><option>Pesquisa</option><option>Artigo</option><option>Livro</option><option>Disciplina</option><option>Evento</option><option>Orientação</option></select><select id="pstatus" className="rounded-xl border bg-background p-2"><option>Planejamento</option><option>Em andamento</option><option>Concluído</option></select><Button onClick={()=>{s.addProject({name,type:(document.getElementById('ptype') as HTMLSelectElement).value as any,status:(document.getElementById('pstatus') as HTMLSelectElement).value as any});setName('')}}>Criar</Button></Card><div className="grid gap-3 md:grid-cols-3">{s.projects.map(p=><Card key={p.id}><b>{p.name}</b><p>{p.type} · {p.status}</p><small>{s.activities.filter(a=>a.projectId===p.id).length} tarefas vinculadas</small></Card>)}</div></div>}
